#include<iostream>
using namespace std;

int main()
{
    int n, i, count;
    n=1; count=0;
    for(i=2;i<=n/2;i++)
    {
        if(n%i==0)
        count++;
    }
    if(count==0&&n!=1) printf("Prime");
    else printf("Not Prime");
}
