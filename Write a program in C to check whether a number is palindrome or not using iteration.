#include<iostream>
using namespace std;
int main()
{
    int num, temp, digit, sum;
    num=3223;
    sum=0;
    temp=num;
    while(num!=0)
    {
       sum=(sum*10)+(num%10);
       num=num/10;
    }
    if(temp==sum) printf("Palindrome!");
    else printf("Not palindrome!");
}
