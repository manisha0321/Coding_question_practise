#include<iostream>
using namespace std;

int main()
{
    int n, i, sum;
    n=8; sum=0;
    for(i=1;i<=n/2;i++)
    {
        if(n%i==0)
        sum=sum+i;
    }
    if(sum==n) printf("Perfect");
    else printf("Not Perfect");
}
