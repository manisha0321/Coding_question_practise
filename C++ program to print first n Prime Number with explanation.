#include<iostream>
using namespace std;

int main()
{
    int i,j, flag,n;
    n=26; j=0, i=1;
    flag=0;
    while(i<=n)
    {   flag=0;
        for(j=2;j<i/2;j++) 
            {
                if(i%j==0) 
                {
                    flag=1; break;
                }
            }
           if(flag==0) printf("%d ",i);
           i++;
    }
}
